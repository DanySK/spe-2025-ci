name: My First Workflow
on:
  push:
    branches:
      - master
    paths-ignore:
      - README.md
  pull_request:

jobs:
  print-a-string:
    runs-on: ubuntu-24.04
    steps:
      - name: Hello World
        run: echo Hello, World!
      - name: Print Gradle Tasks
        run: ./gradlew tasks
